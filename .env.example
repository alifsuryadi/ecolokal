# Server Configuration
APP_PORT=8080
APP_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=ecolokal
DB_SSLMODE=disable

# JWT Configuration
JWT_SECRET=your-super-secret-key-here
JWT_EXPIRE_HOURS=24

# Railway deployment (akan diisi saat deploy)
# DATABASE_URL=postgresql://...